# 🚀 Quick Start Guide

เริ่มต้นใช้งาน Dashboard ภายใน 5 นาที!

## 📋 ขั้นตอนที่ 1: ติดตั้ง Dependencies

```bash
cd FrontEndV2
npm install
```

รอสักครู่ (ประมาณ 30-60 วินาที) dependencies จะติดตั้งเสร็จ

---

## 🚀 ขั้นตอนที่ 2: รัน Development Server

```bash
npm run dev
```

เปิดเบราว์เซอร์ไปที่: **http://localhost:5173**

---

## ✨ ขั้นตอนที่ 3: ทดสอบฟีเจอร์

### 1. ทดสอบ Drag & Drop
- ลองคลิกที่ header ของ Widget (บริเวณที่มี 🔴🟡🟢)
- ลากไปวางที่ต่างๆ
- Widget จะย้ายตำแหน่งตามที่ลาก

### 2. ทดสอบ Resize
- ลองลากจากมุมของ Widget
- Widget จะขยาย/หดได้
- ไม่สามารถหดเล็กกว่าขนาดขั้นต่ำ (มี minWidth/minHeight)

### 3. ทดสอบ Charts
- **Pie Chart** - ดูสัดส่วนพอร์ตโฟลิโอ
- **Bar Chart** - ดูปริมาณการเทรด
- **Line Chart** - ดูยอดคงเหลือ
- **Column Chart** - ดูกำไร/ขาดทุน
- **TradingView** - ลอง Zoom (scroll) และ Pan (ลาก)

### 4. ทดสอบ Table
- **Sort** - คลิกที่หัวคอลัมน์
- **Resize** - ลากขอบคอลัมน์
- **Expand** - คลิกที่ลูกศร (→)
- **Pagination** - กดปุ่ม ก่อนหน้า/ถัดไป

### 5. ทดสอบ Date Range
- เปลี่ยน Date Range จาก Dropdown ด้านบน
- ดูข้อมูลที่เปลี่ยนไปใต้ Header

---

## 🎨 ขั้นตอนที่ 4: Customize (ถ้าต้องการ)

### เปลี่ยนสี
แก้ไขไฟล์ `tailwind.config.js`:

```javascript
colors: {
  primary: {
    500: '#0ea5e9', // เปลี่ยนเป็นสีที่ต้องการ
  }
}
```

### เปลี่ยน Mock Data
แก้ไขไฟล์ `src/data/mockData.ts`:

```typescript
export const portfolioDistribution = {
  labels: ['BTC', 'ETH', 'BNB'], // เปลี่ยนชื่อ
  series: [50, 30, 20]           // เปลี่ยนค่า
};
```

### เพิ่ม Widget
แก้ไขไฟล์ `src/App.tsx`:

```typescript
const widgets = [
  // ... widgets เดิม
  {
    id: 'new-widget',
    title: 'Widget ใหม่',
    component: <YourNewComponent />
  }
];
```

---

## 📦 ขั้นตอนที่ 5: Build สำหรับ Production

```bash
npm run build
```

ไฟล์ที่ Build เสร็จจะอยู่ในโฟลเดอร์ `dist/`

### Preview Production Build
```bash
npm run preview
```

---

## 🔧 Troubleshooting

### ปัญหา: Port 5173 ถูกใช้งานแล้ว
**วิธีแก้:**
```bash
# กด Ctrl+C เพื่อหยุด server
# แล้วรันใหม่
npm run dev
```

### ปัญหา: Module not found
**วิธีแก้:**
```bash
# ลบ node_modules และติดตั้งใหม่
rm -rf node_modules
npm install
```

### ปัญหา: TailwindCSS ไม่ทำงาน
**ตรวจสอบ:**
1. มีไฟล์ `tailwind.config.js` หรือไม่
2. มี `postcss.config.js` หรือไม่
3. มี `@tailwind` directives ใน `index.css` หรือไม่

### ปัญหา: Charts ไม่แสดง
**ตรวจสอบ:**
1. ติดตั้ง `apexcharts` และ `react-apexcharts` แล้วหรือยัง
2. ติดตั้ง `lightweight-charts` แล้วหรือยัง
3. ลองรีเฟรชหน้าเบราว์เซอร์

---

## 📚 เอกสารเพิ่มเติม

- [README.md](./README.md) - ข้อมูลโปรเจคทั่วไป
- [FEATURES.md](./FEATURES.md) - รายละเอียดฟีเจอร์ทั้งหมด
- [TailwindCSS Docs](https://tailwindcss.com/docs)
- [ApexCharts Docs](https://apexcharts.com/docs/)
- [TradingView Lightweight Charts](https://tradingview.github.io/lightweight-charts/)
- [React Grid Layout](https://github.com/react-grid-layout/react-grid-layout)

---

## 🎯 คำถามที่พบบ่อย (FAQ)

### Q: ใช้ Backend จาก FrontEndV2/BackEnd ได้ไหม?
**A:** ได้! เพียงแก้ไข Mock Data เป็นการเรียก API แทน

### Q: เปลี่ยนภาษาเป็นอังกฤษได้ไหม?
**A:** ได้! แก้ไข Labels และ Text ใน Components

### Q: รองรับ Mobile ไหม?
**A:** ใช่! Responsive ทุกขนาดหน้าจอ

### Q: สามารถเพิ่ม Chart อื่นได้ไหม?
**A:** ได้! ApexCharts รองรับ Chart หลายประเภท

### Q: Deploy ยังไง?
**A:** Build แล้ว Upload โฟลเดอร์ `dist/` ไปที่ Server หรือใช้ Vercel/Netlify

---

## 🎉 เริ่มต้นได้เลย!

```bash
cd FrontEndV2
npm install
npm run dev
```

**Happy Coding! 🚀**

