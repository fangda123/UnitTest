# 🚀 คู่มือติดตั้งและแก้ไขปัญหา

## ✅ สถานะปัจจุบัน: พร้อมใช้งาน!

โปรเจคนี้ **Build สำเร็จแล้ว** และพร้อมรัน!

---

## 📋 ขั้นตอนการรัน (สำหรับผู้ใช้ใหม่)

### 1. ตรวจสอบว่าติดตั้ง Dependencies แล้วหรือยัง

```bash
cd /Users/js/Desktop/UnitTest/FrontEndV2
ls node_modules/
```

ถ้ามี folder `node_modules` แสดงว่าติดตั้งแล้ว  
ถ้ายังไม่มี ให้รันคำสั่ง:

```bash
npm install
```

### 2. รัน Development Server

```bash
npm run dev
```

### 3. เปิดเบราว์เซอร์

ไปที่: **http://localhost:5173**

---

## 🔧 แก้ไขปัญหาที่พบบ่อย

### ปัญหา: "รันไม่ได้"

#### สาเหตุที่ 1: Port ถูกใช้งานอยู่แล้ว

**วิธีแก้:**
```bash
# หยุด process เดิม (กด Ctrl+C)
# แล้วรันใหม่
npm run dev
```

หรือเปลี่ยน port:
```bash
npm run dev -- --port 3000
```

#### สาเหตุที่ 2: Node modules ไม่สมบูรณ์

**วิธีแก้:**
```bash
# ลบ node_modules และติดตั้งใหม่
rm -rf node_modules package-lock.json
npm install
npm run dev
```

#### สาเหตุที่ 3: Permission denied

**วิธีแก้:**
```bash
# ใช้ sudo (macOS/Linux)
sudo npm run dev

# หรือเปลี่ยน ownership
sudo chown -R $USER:$USER .
```

---

## ⚠️ Error Messages และวิธีแก้

### Error: "EADDRINUSE: address already in use"

**ความหมาย:** Port 5173 ถูกใช้งานแล้ว

**วิธีแก้:**
```bash
# หา process ที่ใช้ port
lsof -ti:5173

# Kill process
kill -9 $(lsof -ti:5173)

# รันใหม่
npm run dev
```

### Error: "Cannot find module..."

**ความหมาย:** Dependencies ไม่ครบ

**วิธีแก้:**
```bash
npm install
```

### Error: "Vite requires Node.js version 20.19+"

**ความหมาย:** Node.js version เก่าเกินไป (ไม่เป็นปัญหาร้ายแรง)

**วิธีแก้ (ถ้าต้องการ):**
```bash
# ติดตั้ง nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# ติดตั้ง Node.js ใหม่
nvm install 20
nvm use 20

# รันใหม่
npm run dev
```

**หมายเหตุ:** โปรเจคยังรันได้ปกติกับ Node 18.x (แค่จะมี warning)

---

## 🐛 Debug Mode

### ดู Error ละเอียด

```bash
# รัน dev พร้อม debug
npm run dev --verbose

# หรือดู log
npm run dev 2>&1 | tee dev.log
```

### ตรวจสอบ Dependencies

```bash
# ดู dependencies ที่ติดตั้ง
npm list

# ตรวจสอบ outdated packages
npm outdated

# Audit security
npm audit
```

---

## 🔍 Checklist การแก้ปัญหา

ถ้ารันไม่ได้ ให้ตรวจสอบตามลำดับ:

- [ ] 1. อยู่ใน directory ที่ถูกต้องหรือไม่? (`/Users/js/Desktop/UnitTest/FrontEndV2`)
- [ ] 2. มี `node_modules/` หรือยัง? (ถ้าไม่มีให้รัน `npm install`)
- [ ] 3. มี `package.json` หรือไม่?
- [ ] 4. Port 5173 ว่างหรือไม่? (ลอง kill process เก่า)
- [ ] 5. มี Permission หรือไม่? (ลองใช้ `sudo`)
- [ ] 6. Internet ทำงานหรือไม่? (สำหรับ npm install)

---

## 💻 สำหรับ Developer

### Build สำหรับ Production

```bash
npm run build
```

ไฟล์จะอยู่ใน `dist/`

### Preview Production Build

```bash
npm run preview
```

### Run Tests (ถ้ามี)

```bash
npm test
```

### Lint Code

```bash
npm run lint
```

---

## 📦 Dependencies ที่จำเป็น

### Production Dependencies
- react@19.1.1
- react-dom@19.1.1
- apexcharts@5.3.6
- react-apexcharts@1.8.0
- lightweight-charts@latest
- react-grid-layout@1.5.2
- lucide-react@0.553.0
- date-fns@4.1.0

### Development Dependencies
- vite@7.2.2
- typescript@5.9.3
- tailwindcss@3.x
- @types/react@19.1.16
- @types/react-dom@19.1.9
- @types/react-grid-layout@latest
- eslint@9.36.0

---

## 🚀 Quick Fix (ถ้าทุกอย่างพัง)

```bash
# เริ่มต้นใหม่หมด
cd /Users/js/Desktop/UnitTest/FrontEndV2

# ลบทุกอย่าง
rm -rf node_modules package-lock.json dist

# ติดตั้งใหม่
npm install

# Build
npm run build

# รัน
npm run dev
```

---

## 📞 ขอความช่วยเหลือ

ถ้ายังแก้ไม่ได้ ให้ส่ง error message มาพร้อมกับ:

1. Output จาก `npm run dev`
2. Node.js version (`node --version`)
3. NPM version (`npm --version`)
4. Operating System
5. Screenshot (ถ้ามี)

---

## ✅ ตรวจสอบว่าทำงานถูกต้อง

เมื่อรันสำเร็จ คุณจะเห็น:

```
VITE v7.2.2  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
➜  press h + enter to show help
```

จากนั้นเปิดเบราว์เซอร์ไปที่ http://localhost:5173

---

**Happy Coding! 🚀**

