# 🏗 การออกแบบสถาปัตยกรรม (Architecture Design)

## 📋 สารบัญ

1. [สถาปัตยกรรมภาพรวม](#สถาปัตยกรรมภาพรวม)
2. [Monolithic vs Microservices](#monolithic-vs-microservices)
3. [Database Design](#database-design)
4. [API Design](#api-design)
5. [Security Architecture](#security-architecture)
6. [Scalability & Performance](#scalability--performance)

---

## 🏛 สถาปัตยกรรมภาพรวม

### System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                        Client Layer                          │
│  (Web Browser, Mobile App, Postman, Other Services)         │
└────────────────────┬────────────────────────────────────────┘
                     │ HTTP/HTTPS + WebSocket
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                     API Gateway Layer                        │
│  - Rate Limiting                                             │
│  - CORS                                                      │
│  - Security Headers (Helmet)                                 │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                   Application Layer                          │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Routes     │  │ Controllers  │  │  Middleware  │     │
│  │  (Express)   │  │   (Logic)    │  │  (Auth/Val)  │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐                        │
│  │   Models     │  │   Services   │                        │
│  │  (Mongoose)  │  │  (External)  │                        │
│  └──────────────┘  └──────────────┘                        │
└────────────────────┬───────────────────┬────────────────────┘
                     │                   │
         ┌───────────┴───────┐     ┌─────┴──────┐
         ▼                   ▼     ▼            ▼
  ┌──────────┐       ┌──────────┐ ┌──────────┐ │
  │ MongoDB  │       │  Redis   │ │ Binance  │ │
  │(Database)│       │ (Cache)  │ │   API    │ │
  └──────────┘       └──────────┘ └──────────┘ │
                                                 │
                                          ┌──────────┐
                                          │WebSocket │
                                          │ Service  │
                                          └──────────┘
```

### Technology Stack

```
Frontend (ไม่ได้ implement ในโปรเจกต์นี้)
    ↓
┌─────────────────────────────────────────┐
│         Express.js API Server            │
├─────────────────────────────────────────┤
│  • Node.js Runtime                      │
│  • Express.js Framework                 │
│  • JWT Authentication                   │
│  • WebSocket (ws)                       │
│  • Validation (Joi)                     │
└─────────────────────────────────────────┘
    ↓                    ↓
┌──────────┐      ┌──────────┐
│ MongoDB  │      │  Redis   │
│  (Main)  │      │ (Cache)  │
└──────────┘      └──────────┘
    ↓
External APIs
  • Binance REST API
  • Binance WebSocket
```

---

## 🔄 Monolithic vs Microservices

### 1. สถาปัตยกรรมแบบ Monolithic (ที่ใช้ในโปรเจกต์นี้)

```
┌─────────────────────────────────────────────────┐
│           Single Application (Node.js)          │
│                                                 │
│  ┌─────────────┐  ┌─────────────┐             │
│  │    Auth     │  │    User     │             │
│  │   Module    │  │   Module    │             │
│  └─────────────┘  └─────────────┘             │
│                                                 │
│  ┌─────────────┐  ┌─────────────┐             │
│  │   Crypto    │  │  Dashboard  │             │
│  │   Module    │  │   Module    │             │
│  └─────────────┘  └─────────────┘             │
│                                                 │
│           Single Database (MongoDB)            │
└─────────────────────────────────────────────────┘
```

#### ✅ ข้อดี

1. **ความเรียบง่าย (Simplicity)**
   - โครงสร้างโค้ดอยู่ในที่เดียว
   - เข้าใจและพัฒนาได้ง่าย
   - ไม่ต้องจัดการ inter-service communication

2. **Development & Deployment**
   - Build และ deploy ง่ายกว่า
   - ไม่ต้องจัดการหลาย repositories
   - Local development ง่าย (run เพียง 1 process)

3. **Performance**
   - ไม่มี network latency ระหว่าง services
   - Transaction ใน single database ทำงานได้ดี
   - ไม่มี overhead จาก service-to-service calls

4. **Cost Effective**
   - ใช้ resources น้อยกว่า
   - Infrastructure ง่ายกว่า
   - เหมาะกับโปรเจกต์ขนาดเล็ก-กลาง

#### ❌ ข้อเสีย

1. **Scalability**
   - Scale ทั้ง application แม้ต้องการเพียงส่วนเดียว
   - ไม่สามารถ scale แต่ละ module แยกกันได้

2. **Maintenance**
   - Codebase ใหญ่ขึ้นเรื่อยๆ เมื่อโปรเจกต์โต
   - Dependency conflict อาจเกิดขึ้นได้

3. **Deployment**
   - แก้ส่วนเล็กน้อยต้อง deploy ทั้ง app
   - Downtime เมื่อ deploy

4. **Team Collaboration**
   - หลายทีมทำงานใน codebase เดียวกันยาก
   - Code conflict เกิดได้บ่อย

---

### 2. สถาปัตยกรรมแบบ Microservices (ทางเลือก)

```
┌───────────────────────────────────────────────────────┐
│                   API Gateway                          │
│         (Routing, Auth, Rate Limiting)                 │
└──────┬──────────┬──────────┬──────────┬───────────────┘
       │          │          │          │
       ▼          ▼          ▼          ▼
┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│   Auth   │ │   User   │ │  Crypto  │ │Dashboard │
│ Service  │ │ Service  │ │ Service  │ │ Service  │
└────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘
     │            │            │            │
     ▼            ▼            ▼            ▼
┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐
│MongoDB  │  │MongoDB  │  │MongoDB  │  │ Redis   │
│ (Auth)  │  │ (User)  │  │(Crypto) │  │ (Cache) │
└─────────┘  └─────────┘  └─────────┘  └─────────┘
     │            │            │            │
     └────────────┴────────────┴────────────┘
              Message Queue (RabbitMQ/Kafka)
```

#### ✅ ข้อดี

1. **Independent Scaling**
   - Scale แต่ละ service ตามความต้องการ
   - เช่น: Crypto Service ใช้งานเยอะ scale เฉพาะส่วนนั้น

2. **Technology Freedom**
   - แต่ละ service ใช้เทคโนโลยีต่างกันได้
   - เช่น: Auth Service ใช้ Node.js, Analytics Service ใช้ Python

3. **Fault Isolation**
   - Service หนึ่ง down ไม่กระทบ service อื่น
   - Easier to identify and fix issues

4. **Team Autonomy**
   - แต่ละทีมดูแล service ของตัวเอง
   - Deploy independently
   - ลด code conflict

5. **Deployment**
   - Deploy แต่ละ service แยกกัน
   - Zero downtime deployment ทำได้ง่าย
   - Rollback ได้เฉพาะ service ที่มีปัญหา

#### ❌ ข้อเสีย

1. **Complexity**
   - ระบบซับซ้อนมากขึ้น
   - ต้องจัดการหลาย services
   - Infrastructure overhead

2. **Communication**
   - Network latency ระหว่าง services
   - ต้องใช้ message queue หรือ service mesh
   - Distributed transactions ยากขึ้น

3. **Development**
   - Local development ยากกว่า (ต้อง run หลาย services)
   - Testing ซับซ้อนขึ้น (integration testing)

4. **Monitoring & Debugging**
   - ต้องใช้ distributed tracing
   - Log aggregation ซับซ้อน
   - Debugging ข้าม services ยาก

5. **Cost**
   - Infrastructure cost สูงกว่า
   - ต้องการ DevOps skill มากขึ้น

---

## 📊 การเปรียบเทียบ

| ด้าน | Monolithic | Microservices |
|------|------------|---------------|
| **ความซับซ้อน** | 🟢 ต่ำ | 🔴 สูง |
| **Development Speed** | 🟢 เร็ว (ตอนเริ่มต้น) | 🔴 ช้า (setup มาก) |
| **Scalability** | 🔴 จำกัด | 🟢 ยืดหยุ่น |
| **Deployment** | 🟡 กลาง | 🟢 ยืดหยุ่น |
| **Team Size** | 🟢 เล็ก-กลาง | 🟡 กลาง-ใหญ่ |
| **Cost** | 🟢 ต่ำ | 🔴 สูง |
| **Performance** | 🟢 ดี | 🟡 Network overhead |
| **Maintenance** | 🟡 โตขึ้นยากขึ้น | 🟢 แยกเป็นส่วนๆ |

---

## 🎯 เหตุผลที่เลือก Monolithic

สำหรับโปรเจกต์นี้ เลือกใช้สถาปัตยกรรมแบบ **Monolithic** เพราะ:

1. **ขนาดโปรเจกต์**: ขนาดเล็ก-กลาง เหมาะกับ Monolithic
2. **ทีมงาน**: ทีมเล็ก ไม่จำเป็นต้องแยก services
3. **Time to Market**: พัฒนาและ deploy ได้เร็วกว่า
4. **Cost**: Infrastructure cost ต่ำกว่า
5. **Complexity**: หลีกเลี่ยงความซับซ้อนที่ไม่จำเป็น

### 🔄 Migration Path

เมื่อโปรเจกต์โตขึ้น สามารถ migrate ไปเป็น Microservices ได้:

```
Phase 1: Monolithic
    ↓
Phase 2: Modular Monolithic (แยก modules ให้ชัดเจน)
    ↓
Phase 3: Hybrid (แยก service ที่ต้องการ scale เช่น Crypto Service)
    ↓
Phase 4: Full Microservices
```

---

## 💾 Database Design

### Schema Overview

```
┌─────────────────┐
│      User       │
├─────────────────┤
│ _id: ObjectId   │
│ username: String│
│ email: String   │
│ password: String│ (hashed)
│ firstName       │
│ lastName        │
│ role: enum      │ (user/admin)
│ isActive: bool  │
│ profile: Object │
│ createdAt       │
│ updatedAt       │
└────────┬────────┘
         │
         │ 1:N
         ▼
┌─────────────────┐
│      Trade      │
├─────────────────┤
│ _id: ObjectId   │
│ userId: ref     │──> User._id
│ symbol: String  │
│ type: enum      │ (buy/sell)
│ price: Number   │
│ amount: Number  │
│ total: Number   │
│ status: enum    │
│ fee: Number     │
│ tradeDate       │
│ createdAt       │
│ updatedAt       │
└─────────────────┘

┌─────────────────┐
│  CryptoPrice    │
├─────────────────┤
│ _id: ObjectId   │
│ symbol: String  │ (BTCUSDT)
│ price: Number   │
│ highPrice24h    │
│ lowPrice24h     │
│ volume24h       │
│ priceChange%    │
│ openPrice24h    │
│ lastUpdate      │
│ source: enum    │ (api/websocket)
│ createdAt       │
│ updatedAt       │
└─────────────────┘
```

### Indexes

```javascript
// User Collection
{
  email: 1,        // Unique index
  username: 1      // Unique index
}

// Trade Collection
{
  userId: 1, createdAt: -1,    // Compound index
  symbol: 1, createdAt: -1,    // Compound index
  status: 1                     // Single index
}

// CryptoPrice Collection
{
  symbol: 1, createdAt: -1     // Compound index
}
```

### Data Relationships

- **User ↔ Trade**: One-to-Many (1:N)
  - หนึ่ง User มีหลาย Trades
  - ใช้ `userId` field ใน Trade collection

- **CryptoPrice**: Independent
  - ไม่มีความสัมพันธ์กับ collections อื่น
  - Time-series data

---

## 🔐 Security Architecture

### 1. Authentication Flow

```
┌────────┐                           ┌────────┐
│ Client │                           │ Server │
└───┬────┘                           └───┬────┘
    │                                    │
    │  POST /api/auth/register           │
    │  { email, password, ... }          │
    │───────────────────────────────────>│
    │                                    │
    │                                    │ 1. Validate input
    │                                    │ 2. Hash password
    │                                    │ 3. Save to DB
    │                                    │ 4. Generate JWT
    │                                    │
    │  { user, token }                   │
    │<───────────────────────────────────│
    │                                    │
    │  POST /api/auth/login              │
    │  { email, password }               │
    │───────────────────────────────────>│
    │                                    │
    │                                    │ 1. Find user
    │                                    │ 2. Compare password
    │                                    │ 3. Generate JWT
    │                                    │
    │  { user, token }                   │
    │<───────────────────────────────────│
    │                                    │
    │  GET /api/users                    │
    │  Authorization: Bearer <token>     │
    │───────────────────────────────────>│
    │                                    │
    │                                    │ 1. Verify JWT
    │                                    │ 2. Check permissions
    │                                    │ 3. Return data
    │                                    │
    │  { users: [...] }                  │
    │<───────────────────────────────────│
```

### 2. Security Layers

```
┌─────────────────────────────────────────────┐
│  1. Network Layer                           │
│     • HTTPS (TLS/SSL)                       │
│     • CORS configuration                    │
└─────────────────────────────────────────────┘
┌─────────────────────────────────────────────┐
│  2. Application Layer                       │
│     • Helmet (Security Headers)             │
│     • Rate Limiting                         │
│     • Input Validation (Joi)                │
└─────────────────────────────────────────────┘
┌─────────────────────────────────────────────┐
│  3. Authentication Layer                    │
│     • JWT Tokens                            │
│     • Password Hashing (bcrypt)             │
│     • API Key (Internal APIs)               │
└─────────────────────────────────────────────┘
┌─────────────────────────────────────────────┐
│  4. Authorization Layer                     │
│     • Role-Based Access Control (RBAC)      │
│     • Resource ownership checks             │
└─────────────────────────────────────────────┘
┌─────────────────────────────────────────────┐
│  5. Data Layer                              │
│     • MongoDB access controls               │
│     • Sensitive data encryption             │
└─────────────────────────────────────────────┘
```

---

## 🚀 Scalability & Performance

### 1. Caching Strategy

```
┌────────┐                    ┌────────┐
│ Client │                    │  API   │
└───┬────┘                    └───┬────┘
    │                             │
    │  GET /crypto/price/BTCUSDT  │
    │────────────────────────────>│
    │                             │
    │                             ├─1. Check Redis
    │                             │
    │                     ┌───────▼────────┐
    │                     │     Redis      │
    │                     │  (Cache Hit?)  │
    │                     └───────┬────────┘
    │                             │
    │               ┌─────────────┴─────────────┐
    │               │                           │
    │           HIT │                       MISS│
    │               │                           │
    │      ┌────────▼────────┐         ┌────────▼────────┐
    │      │ Return Cached   │         │  Query MongoDB  │
    │      │      Data       │         │  + Cache Result │
    │      └────────┬────────┘         └────────┬────────┘
    │               │                           │
    │               └─────────────┬─────────────┘
    │                             │
    │  { price data, cached }     │
    │<────────────────────────────│
```

### 2. Rate Limiting Strategy

```javascript
// API Endpoints: 100 requests / 15 minutes
// Auth Endpoints: 5 requests / 15 minutes
// Register: 3 requests / 1 hour
```

### 3. Database Optimization

- **Indexes**: ทุก query ที่สำคัญมี index
- **Connection Pooling**: Mongoose default pooling
- **Query Optimization**: ใช้ select เฉพาะ field ที่ต้องการ
- **Aggregation**: ใช้ MongoDB aggregation pipeline

### 4. Horizontal Scaling

```
             ┌──────────────┐
             │ Load Balancer│
             └──────┬───────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
   ┌────▼────┐ ┌───▼────┐ ┌───▼────┐
   │ Node.js │ │Node.js │ │Node.js │
   │Instance1│ │Instance2│ │Instance3│
   └────┬────┘ └────┬───┘ └───┬────┘
        │           │          │
        └───────────┼──────────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
   ┌────▼────┐ ┌───▼────┐ ┌───▼────┐
   │ MongoDB │ │ Redis  │ │Binance │
   │Cluster  │ │Cluster │ │  API   │
   └─────────┘ └────────┘ └────────┘
```

---

## 📈 Monitoring & Logging

### 1. Logging Strategy

```javascript
// Winston Logger Configuration
// Levels: error, warn, info, debug

// Production: error.log, combined.log
// Development: Console output
```

### 2. Health Checks

```
GET /api/health

Response:
{
  "success": true,
  "message": "API กำลังทำงานปกติ",
  "timestamp": "2024-01-01T00:00:00.000Z",
  "uptime": 3600
}
```

---

## 🎯 สรุป

โปรเจกต์นี้ใช้สถาปัตยกรรมแบบ **Monolithic** ที่เหมาะสมกับ:
- โปรเจกต์ขนาดเล็ก-กลาง
- ทีมพัฒนาขนาดเล็ก
- ต้องการ time-to-market เร็ว
- Budget จำกัด

โดยมีการออกแบบที่รองรับการขยายตัวในอนาคต:
- Modular code structure
- Clear separation of concerns
- Caching layer พร้อมแล้ว
- Database indexing ที่เหมาะสม

สามารถ migrate ไปเป็น Microservices ได้เมื่อจำเป็น โดยไม่ต้อง rewrite ทั้งหมด

