# ⚡ Quick Start Guide

## 🚀 เริ่มใช้งานภายใน 3 นาที!

### ขั้นตอนที่ 1: ติดตั้ง Dependencies

```bash
npm install
```

รอประมาณ 30 วินาที... ☕

### ขั้นตอนที่ 2: รัน Development Server

```bash
npm run dev
```

Browser จะเปิดอัตโนมัติที่ `http://localhost:3000` 🎉

### ขั้นตอนที่ 3: เริ่มใช้งาน!

คุณจะเห็น Dashboard ที่มี:

1. **กราฟแท่งเทียน BTC/USDT** (ด้านบนซ้าย)
   - Zoom ด้วยการ Scroll
   - Pan ด้วยการลากเมาส์
   
2. **กราฟวงกลม Portfolio** (ด้านบนขวา)
   - Hover เพื่อดู Highlight
   
3. **กราฟกำไร/ขาดทุน** (ขวาล่าง)
   - แสดงข้อมูลรายสัปดาห์
   
4. **กราฟราคา Bitcoin** (กลาง)
   - แสดงข้อมูล 30 วัน
   
5. **กราฟปริมาณการซื้อขาย** (กลาง)
   - แสดงข้อมูลรายเดือน
   
6. **ตารางประวัติการซื้อขาย** (ด้านล่าง)
   - คลิกแถวเพื่อดูรายละเอียด
   - ลากขอบคอลัมน์เพื่อปรับขนาด
   
7. **Date Range Calculator** (ด้านล่างสุด)
   - เลือกช่วงเวลาที่ต้องการ

---

## 🎯 ทดลองฟีเจอร์

### Drag & Drop
1. เลื่อนเมาส์ไปที่ด้านบนของ Panel ใดก็ได้
2. คลิกค้างและลาก
3. ปล่อยเมาส์

### Resize
1. เลื่อนเมาส์ไปที่มุมของ Panel
2. เมื่อเห็น Cursor เปลี่ยน ให้คลิกค้างและลาก
3. ปล่อยเมาส์

### Reset Layout
- คลิกปุ่ม "รีเซ็ต Layout" ที่มุมบนขวา

---

## 📦 Build สำหรับ Production

```bash
npm run build
```

ไฟล์จะถูกสร้างใน folder `dist/`

### Preview Production Build

```bash
npm run preview
```

---

## 🎨 ปรับแต่ง

### เปลี่ยนสี
แก้ไข `tailwind.config.js`:

```javascript
colors: {
  primary: {
    500: '#YOUR_COLOR',
  },
}
```

### เพิ่มข้อมูล
แก้ไข `src/data/mockData.ts`

### เพิ่ม Chart
สร้างไฟล์ใน `src/components/charts/`

---

## 🆘 ช่วยเหลือ

### ปัญหา: Port 3000 ถูกใช้งานแล้ว
```bash
# Vite จะถามว่าจะใช้ Port อื่นไหม
# กด Y เพื่อใช้ Port อื่น
```

### ปัญหา: Charts ไม่แสดงผล
```bash
# ลบ node_modules และติดตั้งใหม่
rm -rf node_modules
npm install
```

### ปัญหา: Build ล้มเหลว
```bash
# ตรวจสอบ TypeScript errors
npm run build
```

---

## 📚 เอกสารเพิ่มเติม

- **README.md** - ภาพรวมโปรเจค
- **USAGE.md** - คู่มือการใช้งานละเอียด
- **EXAMPLES.md** - ตัวอย่าง Code

---

## ✅ Checklist

- [x] ติดตั้ง Dependencies
- [x] รัน Dev Server
- [x] ทดลอง Drag & Drop
- [x] ทดลอง Resize
- [x] ทดลอง Table (Expandable Rows)
- [x] ทดลอง Date Range Calculator
- [x] Build สำเร็จ

---

**เริ่มต้นได้เลย! 🚀**

```bash
npm install && npm run dev
```

